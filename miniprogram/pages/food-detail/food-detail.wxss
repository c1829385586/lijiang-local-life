.detail-swiper { height: 400rpx; }
.detail-img { width: 100%; height: 400rpx; }
.store-name { font-size: 36rpx; font-weight: bold; }
.score-text { font-size: 26rpx; color: #FF9500; }
.store-tags { margin: 16rpx 0; }
.store-address { margin-top: 12rpx; font-size: 26rpx; color: #666; justify-content: space-between; }
.address-nav { color: #FF6B35; font-size: 24rpx; }
.store-desc { font-size: 26rpx; color: #666; line-height: 1.8; }
.dish-list { margin-top: 12rpx; }
.dish-item { display: flex; justify-content: space-between; padding: 16rpx 0; border-bottom: 1rpx solid #f0f0f0; }
.dish-item:last-child { border-bottom: none; }
.dish-name { font-size: 28rpx; }

/* 评价 */
.review-count { font-size: 24rpx; color: #999; margin-left: 8rpx; }
.review-avg { font-size: 24rpx; color: #FF9500; }
.review-list { margin-top: 16rpx; }
.review-item { padding: 16rpx 0; border-bottom: 1rpx solid #f5f5f5; }
.review-item:last-child { border-bottom: none; }
.review-avatar { width: 56rpx; height: 56rpx; border-radius: 50%; flex-shrink: 0; }
.review-meta { margin-left: 12rpx; }
.review-name { font-size: 26rpx; display: block; }
.review-stars { font-size: 20rpx; }
.review-content { font-size: 26rpx; color: #333; margin: 12rpx 0; display: block; line-height: 1.6; }
.empty-mini { text-align: center; padding: 20rpx; }
.empty-mini-text { font-size: 24rpx; color: #ccc; }
.bottom-bar { position: fixed; bottom: 0; left: 0; right: 0; background: #fff; padding: 16rpx 24rpx; padding-bottom: calc(16rpx + env(safe-area-inset-bottom)); box-shadow: 0 -2rpx 12rpx rgba(0,0,0,0.06); display: flex; align-items: center; gap: 24rpx; }
.bottom-actions { display: flex; gap: 32rpx; }
.bottom-action-item { display: flex; flex-direction: column; align-items: center; }
.action-icon { font-size: 36rpx; }
.action-text { font-size: 20rpx; color: #666; margin-top: 4rpx; }
